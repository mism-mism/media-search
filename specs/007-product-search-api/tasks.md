# Tasks: Product search API

- [x] T000 Lock Round 2; 006 UI finish acknowledged; `spec.md` → `active`
- [x] T010 Add `product_id` to domain + sqlite migrate + DTOs
- [x] T020 Text search merge (semantic + display_name/tags); GET+POST
- [x] T030 `POST /api/search/by-image` + shared filters / match_kinds
- [x] T040 Tests (merge, by-image, product_id exact)
- [x] T050 PRODUCT.md / OpenAPI descriptions; verify + post-implement
