# Plan

Add a small action row below the library upload hint. Reuse requestJson,
pollJob, refreshAssets and existing import banner. Share one busy-state setter
between upload and reimport. Preserve backend contracts and generation limits.
Exercise rendered UI JavaScript through an executable DOM/event harness and,
if local browser tooling is available, check the visible button at desktop/mobile.
Update operational instructions to name the actual button. Lean independent
Inner test/code-quality and Outer product reviews, gates, CI and deployment.
